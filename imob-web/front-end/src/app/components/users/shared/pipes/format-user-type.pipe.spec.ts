import { FormatUserTypePipe } from './format-user-type.pipe';

describe('FormatUserTypePipe', () => {
    it('create an instance', () => {
        const pipe = new FormatUserTypePipe();
        expect(pipe).toBeTruthy();
    });
});
