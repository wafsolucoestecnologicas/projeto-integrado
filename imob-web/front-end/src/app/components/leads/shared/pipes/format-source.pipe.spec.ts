import { FormatSourcePipe } from './format-source.pipe';

describe('FormatSourcePipe', () => {
    it('create an instance', () => {
        const pipe = new FormatSourcePipe();
        expect(pipe).toBeTruthy();
    });
});
