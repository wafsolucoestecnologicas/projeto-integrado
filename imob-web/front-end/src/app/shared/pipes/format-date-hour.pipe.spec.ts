import { FormatDateHourPipe } from './format-date-hour.pipe';

describe('FormatDateHourPipe', () => {
    it('create an instance', () => {
        const pipe = new FormatDateHourPipe();
        expect(pipe).toBeTruthy();
    });
});
