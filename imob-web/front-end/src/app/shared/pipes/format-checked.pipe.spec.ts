import { FormatCheckedPipe } from './format-checked.pipe';

describe('FormatCheckedPipe', () => {
    it('create an instance', () => {
        const pipe = new FormatCheckedPipe();
        expect(pipe).toBeTruthy();
    });
});
